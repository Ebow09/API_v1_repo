# API demo
## Description
An API desgined in LUMEN to perform CRUD operation on a database.

It uses an Auth0 token for authentication

This could be extended to:
- allow the scanning, uploading and attaching other documents like photo's or ceetificates 
- other tables like a user table 


